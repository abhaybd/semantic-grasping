hydra:
  run:
    dir: /results

grasp_encoder:
  embed_dim: 4096
  hidden_dim: 512
  feature_layers: [2048, 2048]
  xyz_layers: [256, 256]
  grasp_layers: [256, 256]
  final_fc_layers: [2048, 2048]
  train_vision_model: false
  transformer:
    nhead: 4
    num_encoder_layers: 3

train:
  dataloader:
    batch_size: 96
    num_workers: 4
  dataset:
    augment: true
    augmentation_params:
      color_jitter_prob: 0.8
      gray_scale_prob: 0.1
      horizontal_flip_prob: 0.5
      flip_grasp_prob: 0.5
    data_dir: /data
    csv_path: /dataset/dataset.csv
    text_embedding_path: /dataset/text_embeddings.npy
  optimizer:
    lr: 0.0002
    # weight_decay: 0.0
  test:
    frac: 0.2
    period: 2
    batch_size: 128
  lr_schedule:
    warmup_epochs: 20
    final_factor: 0.1
  epochs: 200
  save_period: 10
  seed: 42
